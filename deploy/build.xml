<project default="install" basedir=".." >
	<property file="${user.home}/work/buildconfigs/latinum/build.properties" />
	<property file="deploy/build.properties" />
	<target name="install">
		<echo>deploy dir: ${deploydir}</echo>

		<delete dir="${deploydir}/shared/latinum" failonerror="false" />
		<copy todir="${deploydir}/shared/latinum">
			<fileset dir="lib" />
		</copy>

	</target>
</project>
